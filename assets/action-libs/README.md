# Action libraries

These `.lib` files provide the GameMaker Studio 1.4 drag-and-drop action library used by the object and timeline editors.

Development builds read this directory directly. Electron Builder copies the `.lib` files to `resources/action-libs` when packaging the application.
